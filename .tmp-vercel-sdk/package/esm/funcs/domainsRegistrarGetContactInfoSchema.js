/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { GetContactInfoSchemaDomainsRegistrarResponseBody$inboundSchema, GetContactInfoSchemaRequest$outboundSchema, GetContactInfoSchemaResponseBody$inboundSchema, } from "../models/getcontactinfoschemaop.js";
import { InternalServerError$inboundSchema, } from "../models/internalservererror.js";
import { NotAuthorizedForScope$inboundSchema, } from "../models/notauthorizedforscope.js";
import { TooManyRequests$inboundSchema, } from "../models/toomanyrequests.js";
import { Unauthorized$inboundSchema, } from "../models/unauthorized.js";
import { APIPromise } from "../types/async.js";
/**
 * Get contact info schema
 *
 * @remarks
 * Some TLDs require additional contact information. Use this endpoint to get the schema for the tld-specific contact information for a domain.
 */
export function domainsRegistrarGetContactInfoSchema(client, request, options) {
    return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
    const parsed = safeParse(request, (value) => GetContactInfoSchemaRequest$outboundSchema.parse(value), "Input validation failed");
    if (!parsed.ok) {
        return [parsed, { status: "invalid" }];
    }
    const payload = parsed.value;
    const body = null;
    const pathParams = {
        domain: encodeSimple("domain", payload.domain, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path = pathToFunc("/v1/registrar/domains/{domain}/contact-info/schema")(pathParams);
    const query = encodeFormQuery({
        "teamId": payload.teamId,
    });
    const headers = new Headers(compactMap({
        Accept: "application/json",
    }));
    const secConfig = await extractSecurity(client._options.bearerToken);
    const securityInput = secConfig == null ? {} : { bearerToken: secConfig };
    const requestSecurity = resolveGlobalSecurity(securityInput);
    const context = {
        options: client._options,
        baseURL: options?.serverURL ?? client._baseURL ?? "",
        operationID: "getContactInfoSchema",
        oAuth2Scopes: null,
        resolvedSecurity: requestSecurity,
        securitySource: client._options.bearerToken,
        retryConfig: options?.retries
            || client._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
    };
    const requestRes = client._createRequest(context, {
        security: requestSecurity,
        method: "GET",
        baseURL: options?.serverURL,
        path: path,
        headers: headers,
        query: query,
        body: body,
        userAgent: client._options.userAgent,
        timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req = requestRes.value;
    const doResult = await client._do(req, {
        context,
        errorCodes: ["400", "401", "403", "429", "4XX", "500", "5XX"],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req }];
    }
    const response = doResult.value;
    const responseFields = {
        HttpMeta: { Response: response, Request: req },
    };
    const [result] = await M.match(M.json(200, GetContactInfoSchemaResponseBody$inboundSchema), M.jsonErr(400, GetContactInfoSchemaDomainsRegistrarResponseBody$inboundSchema), M.jsonErr(401, Unauthorized$inboundSchema), M.jsonErr(403, NotAuthorizedForScope$inboundSchema), M.jsonErr(429, TooManyRequests$inboundSchema), M.jsonErr(500, InternalServerError$inboundSchema), M.fail("4XX"), M.fail("5XX"))(response, req, { extraFields: responseFields });
    if (!result.ok) {
        return [result, { status: "complete", request: req, response }];
    }
    return [result, { status: "complete", request: req, response }];
}
//# sourceMappingURL=domainsRegistrarGetContactInfoSchema.js.map